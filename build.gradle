//Set up general variables.
ext.env = System.getenv()
String name = env.JOB_NAME
println "Packing up " + name
String technicPack = "";

println "Packing Technic Pack"
FileTree tree = fileTree(dir: 'mods').exclude("**/*.gradle")

tree.each
{
	File file ->
	println file
}

task myZip(type: Zip) <<
{
    from 'somedir'
}